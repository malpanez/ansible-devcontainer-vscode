---
# Sample remote inventory for cloud targets. Replace placeholders with real host data.
all:
  children:
    staging:
      hosts:
        web1:
          ansible_host: 203.0.113.10
          ansible_user: ubuntu
          ansible_port: 22
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
          ansible_python_interpreter: /usr/bin/python3
          ansible_become: true
      vars:
        ansible_connection: ssh
