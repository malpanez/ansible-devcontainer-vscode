---
phase: 01-ci-stability
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [CI-01, CI-02, CI-03]

must_haves:
  truths:
    - "CI passes green 3 consecutive times on develop"
    - "No test is marked flaky or skipped due to plugin conflicts"
    - "molecule job appears in CI run history for develop commits"
  artifacts:
    - path: ".planning/phases/01-ci-stability/CI-RUNS.md"
      provides: "Evidence log of 3 green CI runs"
      contains: "Run 1, Run 2, Run 3"
  key_links:
    - from: "develop branch push"
      to: "GitHub Actions CI Pipeline"
      via: "on.push.branches: [develop]"
      pattern: "ci.yml triggers"
---

<objective>
Verify that all CI fixes applied in previous commits (9861121..19a5984) and the structural workflow changes from Plan 01 combine to produce a stable, reproducible pipeline.

The deliverable is 3 consecutive green CI runs on develop, confirming:
- pytest-ansible conflict is resolved (CI-02)
- regex_search / ansible_version tests pass (CI-01)
- molecule runs on develop (CI-03)

Purpose: Confirm that "CI completamente verde y estable" is real, not theoretical.
Output: Evidence log at `.planning/phases/01-ci-stability/CI-RUNS.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger and collect 3 CI run results</name>
  <files>.planning/phases/01-ci-stability/CI-RUNS.md</files>
  <action>
Use the GitHub CLI to trigger CI runs and collect results. The develop branch already has all root-cause fixes. Plan 01 adds the workflow structural changes.

Step 1 — Push an empty commit to trigger CI (if no pending push exists):
```bash
git commit --allow-empty -m "ci: verify 3 consecutive green runs for Phase 1 validation"
git push origin develop
```

Step 2 — Wait for the CI run to complete and collect the run ID:
```bash
gh run list --branch develop --workflow ci.yml --limit 5
```

Step 3 — Check the result of each run:
```bash
gh run view <RUN_ID> --log-failed 2>/dev/null || gh run view <RUN_ID>
```

Step 4 — Repeat for runs 2 and 3. Each run can be triggered by a subsequent empty commit, or by waiting for any in-flight develop commits to complete.

Record each run's: run ID, trigger SHA, timestamp, result (success/failure), and which jobs ran (check that `molecule` appears for ansible-path changes).

Write `.planning/phases/01-ci-stability/CI-RUNS.md` with the following structure:

```markdown
# CI Stability Evidence — Phase 1

| Run | ID | SHA | Timestamp | Result | Molecule ran? |
|-----|----|-----|-----------|--------|---------------|
| 1   | ...| ... | ...       | green  | yes/no        |
| 2   | ...| ... | ...       | green  | yes/no        |
| 3   | ...| ... | ...       | green  | yes/no        |

## Notes
[Any observations about flakiness, skipped jobs, or warnings]
```

If any run fails, diagnose the failure before proceeding. Do NOT mark the run as green if it failed. Investigate and fix the root cause before retrying.
  </action>
  <verify>
    <automated>test -f /workspace/.planning/phases/01-ci-stability/CI-RUNS.md && grep -c "green" /workspace/.planning/phases/01-ci-stability/CI-RUNS.md</automated>
  </verify>
  <done>CI-RUNS.md exists and contains 3 rows all showing "green". The file is committed to the repo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    3 consecutive green CI runs on develop, covering:
    - Python unit tests (3.11 and 3.12 matrix legs)
    - Ansible tests with regex_search and ansible_version (no more subprocess binary call)
    - pytest without argparse conflict (pytest-ansible plugin disabled)
    - molecule job triggered on develop for ansible-path changes
  </what-built>
  <how-to-verify>
    1. Open the GitHub Actions tab for this repo
    2. Filter by branch: develop, workflow: CI Pipeline
    3. Confirm the last 3 runs all show green checkmarks
    4. Click into one run and confirm:
       - "Python Unit Tests (Python 3.11)" job is present and green
       - "Python Unit Tests (Python 3.12)" job is present and green
       - "Molecule" job ran (if ansible paths changed in that commit) and passed
       - "Test Playbooks" job passed without errors about ansible --version
    5. Review CI-RUNS.md at .planning/phases/01-ci-stability/CI-RUNS.md for the evidence log
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 runs are green, or describe which job failed and the error message</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
- CI-RUNS.md documents 3 consecutive green runs
- Human confirmation received via checkpoint
- All three requirements have observable evidence:
  - CI-01: test-playbooks job green (regex_search + ansible_version working)
  - CI-02: unit-tests green without plugin conflict errors
  - CI-03: molecule job visible in CI run history for develop
</verification>

<success_criteria>
- 3 consecutive green CI runs on develop, evidenced in CI-RUNS.md
- No "argparse conflict" or "pytest-ansible" errors in any run
- molecule job appears in CI history for develop branch ansible-path commits
- Human has confirmed via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-ci-stability/01-02-SUMMARY.md` following the summary template.

Also update `.planning/STATE.md`:
- Set Phase 1 status to "Complete"
- Set Phase 2 as current focus
</output>
